# Secrets — NUNCA subir
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Serverless Devs
.s/
.serverless/

# Node / Next.js (fase 4)
node_modules/
.next/
out/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Fallback local de datos runtime (memoria de comprador, aprobaciones) — no versionar
data/_runtime_*.json
data/demo_results.json

# s.yaml se genera desde s.template.yaml con envsubst e incluye secretos
# inyectados — el que se versiona es s.template.yaml, nunca s.yaml.
/s.yaml

# ./python la genera 's build' con todas las dependencias instaladas —
# artefacto de build, no se versiona, se regenera cuando haga falta.
/python/
