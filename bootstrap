#!/bin/bash
# Entrypoint del custom runtime de Alibaba Function Compute.
# FC inyecta el puerto en FC_SERVER_PORT (por defecto 9000). Uvicorn escucha ahí.
PORT=${FC_SERVER_PORT:-9000}
exec uvicorn app.main:app --host 0.0.0.0 --port "$PORT"
